# Copy to .env.local for local dev, or set in Cloudflare Pages dashboard

# Your Render backend URL (get after deploying backend to Render)
REACT_APP_API_URL=https://raven-sharp-optimiser.onrender.com
